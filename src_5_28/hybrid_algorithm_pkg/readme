项目名称： 基于 ROS 2 与运动学约束的混合 A* 路径规划器开发（个人项目）
技术栈： ROS 2, Python, NumPy, SciPy, RViz, Gazebo, Nav2 (Map Server)

项目描述与核心贡献：

    算法核心实现与优化： 基于 Python 从零实现了带运动学约束（Ackermann/差速）的混合 A* (Hybrid A*) 算法。针对纯 Python 环境下的算力瓶颈，重写了底层搜索逻辑；引入 collections.deque 优化 2D BFS 启发函数预计算的队列操作，将时间复杂度降至 O(1)，并彻底解决角度归一化与离散化粒度问题，使规划耗时缩短至 1 秒以内。

    ROS 2 系统集成与通信： 独立完成算法与 ROS 2 框架的深度融合。熟练处理 OccupancyGrid 栅格坐标与物理世界坐标的精准双向转换；针对 ROS 2 的通信机制，正确配置 Transient Local QoS (服务质量) 策略，解决静态地图 (map_server) 话题数据的接收断层问题。

    环境感知与安全策略： 针对真实机器人体积导致的碰撞风险，利用 SciPy 图像形态学工具 (ndimage.binary_dilation) 在地图回调阶段实现实时的障碍物安全膨胀层（Inflation Layer），确保生成的路径严格符合机器人的物理通过性。

    仿真与可视化调试： 配置完善的 TF 坐标变换树，并在 RViz 中实现起点/终点交互与规划路径 (Path 消息) 的实时可视化，打通了从地图解析、算法推演到界面展示的完整闭环。

为什么归一化了角度还是会卡死？

罪魁祸首是你代码里的启发函数 heuristic。
目前用的是欧几里得距离（两点之间的直线距离）。在算法的“眼中”，它就像一根橡皮筋，死死地把机器人往目标点拉。
当机器人面前有一堵墙时，算法依然认为“往前撞”是距离目标最近的。由于机器人有最小转弯半径（无法原地掉头），当它一头扎进死胡同或贴近障碍物时，想要退出来需要极度复杂的“前进-打死方向盘-倒车-打死方向盘”的组合操作。这会导致搜索树瞬间爆炸，产生几万个节点，最终把程序卡死。
终极解决方案：引入 2D 距离启发（Holonomic with Obstacles）

工业界解决这个问题的标准做法是：在考虑汽车运动学之前，先不管车头朝向，把汽车当成一个质点，在带有障碍物的地图上跑一次基础的 2D 寻路（比如 BFS 或 Dijkstra），算出地图上每一个无障碍格子到终点的真实绕行距离。

把这个“真实的绕行距离”作为混合 A* 的启发值（h 值）。这样算法就像开了“透视挂”一样，离墙还有很远的时候就知道前面过不去，提前绕路。




因为使用的是保存好的静态地图 不需要使用cartographer_slam.launch提供动态的信息 gazebo启动文件 Cartographer Node Cartographer Occupancy Grid Node节点已注销
ros2 launch hybrid_algorithm_pkg gazebo_sim.launch.py  
ros2 run hybrid_algorithm_pkg astarplanner 
ros2 run hybrid_algorithm_pkg pure_pursuit_controller 
